{
	"name": "pano-typed",
	"version": "1.0.0",
	"description": "Near-full coverage of Panorama's APIs and data structures with Typescript definitions.",
	"author": "Koerismo",
	"license": "MIT",
	"type": "module",

	"scripts": {
		"build":          "node compile.js shared strata",
		"build:p2ce":     "node compile.js shared,p2ce p2ce",
		"build:momentum": "node compile.js shared,momentum momentum",
		"build:csgo":     "node compile.js shared csgo"
	},

	"devDependencies": {
		"multistream": "^4.1.0",
		"replacestream": "^4.0.3"
	}
}
